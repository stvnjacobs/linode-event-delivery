apiVersion: v1
kind: Service
metadata:
  name: {{ include "linode-event-delivery.fullname" . }}
  labels:
    {{- include "linode-event-delivery.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "linode-event-delivery.selectorLabels" . | nindent 4 }}
